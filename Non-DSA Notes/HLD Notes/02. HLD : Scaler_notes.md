### * DNS : ###

DNS (Domain Name System) is the system that translates human-friendly domain names like
example.com → IP addresses like 142.250.183.78.

 * Why DNS matters in system design:
   - Decouples services from IP addresses
   - Enables load balancing
   - Allows geo-routing
   - Ensures availability via distributed servers
   - Supports scaling and global traffic distribution

  * DNS Hierarchy :
    DNS is hierarchical, distributed, and fault-tolerant:
    - Root servers
      - 13 logical root server clusters (A–M)
      - The top of the DNS hierarchy
      - Managed under ICANN oversight
        
    - TLD (Top-Level Domain) servers
      - .com, .org, .net, .in, .io, etc.
    
    - Authoritative Name Servers (your domain DNS)
      - These hold actual domain → IP mappings
      - E.g., Cloudflare, Route53, GoDaddy, custom DNS
    
    - Recursive Resolvers (DNS resolvers)
      - Usually your ISP, Google DNS (8.8.8.8), Cloudflare (1.1.1.1)
      - They cache results
   
  * How a DNS Query Works
    Example: User visits google.com
    - Browser checks local cache
    - OS checks system DNS cache
    - Query goes to recursive resolver
    - Resolver queries root server
    - Root → gives TLD (.com) servers
    - TLD → gives authoritative name servers
    - Authoritative → returns final IP address
    - Resolver caches it (TTL)
    - Browser connects to the IP

  * DNS is used for:
    - Caching : DNS heavily relies on caching for performance. Local cache, Browser cache, Resolver cache, CDN DNS cache
    - Load Balancing : Weighted DNS, Round-robin DNS, Geo-DNS (Route users to nearest region)
    - High Availability : Primary server, Fallback server (backup region), Failover routing (Route53, Cloudflare)
    - Microservices : A hostname → IP / service discovery mapping, Example: orders-service.internal
    - CDNs : CDNs modify DNS responses to direct users to nearest edge.
      

  * ICANN (Internet Corporation for Assigned Names and Numbers)
    is the global organization responsible for coordinating the DNS root zone and domain namespace.
    ICANN responsibilities:
    - Manages the root DNS servers
    - Approves new TLDs (.dev, .app, .io)
    - Oversees domain registrars (GoDaddy, Namecheap)
    - Allocates IP address blocks via regional registries
    - Maintains global domain governance

**DNS is the globally distributed system that maps domain names to IPs. It is hierarchical—root, TLD, and authoritative servers.
In system design, DNS is vital for load balancing, geo-routing, and failover.
ICANN governs the root DNS infrastructure and manages TLDs and registrars.
For scaling services, I would use a low TTL for dynamic endpoints, use Route53/Cloudflare for health-based routing, and rely on DNS caching for performance.**



* Static vs Dynamic IP :

* Static IP : A Static IP is an IP address that does not change. It is manually assigned and stays fixed until someone reconfigures it.
  * Uses :
    - Servers (web servers, DB servers)
    - Network devices (routers, firewalls, switches)
    - VPN endpoints
    - Email servers
    - Remote access setups
    - IoT devices needing constant reachability
 
  * Advantages :
    - Consistent, reliable address
    - Good for hosting (DNS points to it)
    - Better for remote access and firewall rules
    - Required for some protocols (VoIP, VPN)

   * Disadvantages :
    - Usually costs extra (ISP charges)
    - Higher risk of attack (because address is predictable)
    - Must be manually configured
    - Not scalable for huge dynamic environments

* Dynamic IP : A Dynamic IP is automatically assigned by DHCP (Dynamic Host Configuration Protocol) and can change periodically.
  * Uses :
    - Home Wi-Fi users
    - Internal devices (laptops, phones, tablets)
    - Cloud VMs if not configured as “static”
    - Large networks where devices frequently join/leave
      
   * Advantages :
     - Easier network management
     - More secure (address changes)
     - Efficient reuse of IP addresses
     - No cost (for most home broadband)

   * Disadvantages :
     - IP may change → bad for hosting
     - Not suitable for DNS pointing
     - External services cannot reliably reach the device
     - Firewall rules break if IP changes

-----------------------
-----------------------

* Load Balancing
* Sharding
* vertical scaling
* horizontal scaling

-----------------------
-----------------------

* Decouple storage and Code :
  So it is better to decouple code and storage. However, the only downside of decoupling is the additional latency of going from one machine to another (code to the database).
  * We should decouple business logic and storage :
    - storage will also be unavailable during deployment time.
    - different specialised hardware are required for running code(high gpu, RAM, cpu) and storage(HDD).
    - Scaling will be difficult. both server and storage needs to scaled up together even if it's not required.
    - Only disadvantage is increased latency due to extra network hop for each request from app server to storage machine.





------------------
------------------

* Case Studies :
  - code submission (L3) - caching
  - Contest leader board (L3) - caching
  - FB News Feed (L3)












