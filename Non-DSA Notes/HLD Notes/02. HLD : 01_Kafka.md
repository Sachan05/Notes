
* ## KAFKA : ##

* **Apache Kafka is a publish-subscribe messaging system**. A messaging system lets you send messages between processes, applications, and servers. Broadly Speaking, Apache Kafka is software where topics (a topic might be a category) can be defined and further processed. Applications may connect to this system and transfer a message onto the topic. A message can include any kind of information from any event on your blog or can be a very simple text message that would trigger any other event.
* **Asynchronous Communication:** Message queues enable asynchronous interactions between services, reducing the tight coupling and dependencies that occur when services wait for each other to respond before proceeding .
  
* Kafka is an open-source messaging system that was created by LinkedIn and later donated to the Apache Software Foundation. It's built to handle large amounts of data in real time, making it perfect for creating systems that respond to events as they happen. Kafka organizes data into categories called "topics." Producers (apps that send data) put messages into these topics, and consumers (apps that read data) receive them. Kafka ensures that the system is reliable and can keep working even if some parts fail.

* **Core Components :**
  - **Kafka Broker :** A Kafka broker is a server that runs Kafka and stores data. Typically, a Kafka cluster consists of multiple brokers that work together to provide scalability, fault tolerance, and high availability. Each broker is responsible for storing and serving data related to topics. It's used for sharding and replication. It provides master/slave configuration. within kafka, there are thousands of brokers, each broker has multiple copies of topics and partitions, somewhere master, somewhere slave. all this configuration is managed by zookeeper. it also supports leader election algorithm for providing master-slave architecture. earlier versions of kafka used zookeeper which is now being deprecated and currently it uses K-RAFT.
  - **Producers :** A producer is an application or service that sends messages to a Kafka topic. These processes push data into the Kafka system. Producers decide which topic the message should go to, and Kafka efficiently handles it based on the partitioning strategy.
  - **Kafka Topic :** A topic in Kafka is a category or feed name to which messages are published. Kafka messages are always associated with topics, and when you want to send a message, you send it to a specific topic. Topics are divided into partitions, which allow Kafka to scale horizontally and handle large volumes of data. It allows you to split data across multiple brokers.
  - **Consumers and Consumer Groups :** A Consumer is an application that reads messages from Kafka topics. Kafka allows consumer groups, where multiple consumers can read from the same topic, but Kafka ensures that each message is processed by only one consumer in the group. This helps with load balancing and allows consumers to read messages starting from any offset.
    - **Partitions allow you to parallelize a topic by splitting the data in a particular topic across multiple brokers.**
  - **Zookeeper :** Kafka uses Apache ZooKeeper to manage metadata, control access to Kafka resources, and handle leader election and broker coordination. ZooKeeper ensures high availability by making sure the Kafka cluster remains functional even if a broker fails. ZooKeeper acts as a distributed coordinating service that manages various configurations across nodes in a distributed system like Apache Kafka.

* **Need :**
  - **Real-Time Processing:** Kafka is optimized for handling real-time data streams, allowing businesses to process and act on data as it happens.
  - **Fault-Tolerant:** Kafka ensures that even if parts of the system fail, data won’t be lost, making it a highly reliable messaging system.
  - **Scalable:** Kafka scales horizontally by adding more brokers, allowing it to handle growing data loads and increasing numbers of producers and consumers.
  - **Event-Driven Architecture:** Kafka powers event-driven architectures, enabling systems to respond to events in real-time without having to constantly poll for changes.
 
  - **Use Cases of Message Queues :**
    - **Microservices Communications:** Message queues facilitate communication between microservices without requiring direct service-to-service calls, thereby reducing dependency-related issues and improving fault tolerance. (Asynchronous Comm.)
    - you can also scale up individual services based on requirement, it need not be proportionality. for ex: if the order service in amazon increases dur to high demand, we can scale up order service but notification service need not be proportianlly scaled, we can add message queue if we can afford a slight delay for notoifcation service, similary for invoice service, we need not scale upo invoice service as much as order service as we can afford slight delay for it. its for absorbing the impact between two services.
    - **Background Processing:** In use cases such as video processing on YouTube, message queues help manage long-running tasks asynchronously, allowing the system to acknowledge receipt of a task and process it later.

* **Working :**
  - **Step 1: Producers Send Data**
    - Producers are applications that create data and send it to Kafka.
    - This data can be anything—logs, transactions, user activities, or events.
    - Kafka splits the data into smaller parts called partitions, making it easier to handle large amounts of information.
  - **Step 2: Kafka Stores the Data**
    - Kafka organizes the data into topics, where it is saved for a certain period.
    - Even if a consumer reads the data, Kafka doesn’t delete it immediately.
    - To prevent data loss, Kafka makes copies of the data and stores them on different servers.
  - **Step 3: Consumers Read the Data**
    - Consumers are applications that subscribe to topics and read messages.
    - To manage the load, consumers are divided into consumer groups, so no message is processed twice.
    - Consumers can choose where to start reading, whether from the newest message or an earlier point.
  - **Step 4: Kafka Balances the Load**
    - ZooKeeper helps Kafka manage which server is in charge of storing and distributing data.
    - If a server goes down, Kafka automatically redirects the data to another server.
  - **Step 5: Data is Processed and Used**
    - Once consumers receive the data, they can store it in a database, analyze it, or trigger other events.
    - Kafka can work with tools like Apache Spark, Flink, and Hadoop for deeper analysis.



* **How Kafka Integrates Different Data Processing Models** :
  
  -  **Event Streaming (Publish-Subscribe Model) :**
    - Kafka’s primary function is event streaming, where:
    - Producers (applications sending data) publish messages to Kafka topics.
    - Consumers (applications reading data) subscribe to topics and receive messages as soon as they arrive.
    - Multiple consumers can read the same message, allowing for real-time data distribution.
 
  - **Message Queue (Point-to-Point Processing) :**
    - Kafka can also act like a message queue by using consumer groups:
    - When multiple consumers are in the same group, Kafka distributes messages among them, ensuring each message is processed only once.
    - This setup helps in load balancing, making sure no single consumer is overwhelmed.
   
  - **Batch Processing :**
    - Even though Kafka is designed for real-time data, it can also handle batch processing:
    - Messages can be stored in Kafka topics and processed later.
    - Tools like Apache Spark or Hadoop can read data from Kafka in batches and perform analytics.
   
  - **Hybrid Model (Real-Time + Batch Processing) :**
    - Kafka is flexible enough to support a mix of real-time and batch processing:
    - It can send data immediately for real-time analytics while also storing it for batch processing later.
    - This is often done using Kafka Streams, Spark Streaming, or Flink.


* **Apache Kafka vs RabbitMQ :**
  Apache Kafka and RabbitMQ are both popular messaging systems, but they differ significantly in their architecture and use cases:

| Dimension                    | **Apache Kafka**                                      | **RabbitMQ**                                    |
| ---------------------------- | ----------------------------------------------------- | --------------------------------------------------|
| **Architecture**       | Distributed, partitioned, replicated **commit log**   | Broker-based **queue system** with exchanges and queues |
| **Message Model**      | **Publish–subscribe** over append-only logs           | **Queue-based messaging** with exchanges          
| **Persistence**        | Messages **always persisted to disk**                 | In-memory by default, optional disk persistence   |
| **Message Deletion**   | Based on **retention policy** (time/size)             | After **consumer acknowledgment**                 |
| **Scalability**        | **Horizontal scaling** via partitions and brokers     | Limited horizontal scaling; better vertical scaling|
| **Throughput**         | **Very high** (hundreds of thousands to millions/sec) | Moderate                                     |
| **Latency**            | Milliseconds (higher due to batching & disk IO)       | **Sub-millisecond to few ms**                           |
| **Message Replay**     | **Native support** via offsets                        | Not natively supported                                  |
| **Delivery Guarantees**      | At-most-once, At-least-once, **Exactly-once**     At-most-once, At-least-once                             |
| **Consumer Model**           | **Pull-based**                                        | **Push-based**                                    |
| **Consumer State**           | Maintained by **consumer (offsets)**                  | Maintained by **broker**                          |
| **Ordering Guarantee**       | Guaranteed **within a partition**                     | Guaranteed **within a queue**                     |
| **Fan-out Support**          | Multiple consumer groups                              | Exchange-based routing                            |
| **Routing**                  | Limited (topic + partition key)                       | **Rich routing** (direct, topic, fanout, headers) |
| **Protocol Support**         | Custom Kafka TCP protocol                             | **AMQP**, MQTT, STOMP, HTTP                       |
| **Backpressure Handling**    | Natural via pull model                                | Prefetch limits                                   |
| **Message Size Handling**    | Large messages discouraged                            | Handles small/medium messages well                |
| **Operational Complexity**   | **High**                                              | **Lower**                                         |
| **Primary Use Cases**        | Event streaming, logs, analytics pipelines            | Task queues, background jobs                      |
| **Typical Message Lifetime** | Long-lived                                            | Short-lived                                       |
| **Multi-Consumer Support**   | Native and easy                                       | Limited                                           |
| **Exactly-Once Processing**  | Supported (complex)                                   | Not supported                                     |




* the event/task/message is usually a json object which we insert into the queue. for example if we are adding the video to the queue, we dont add the entire video in it, we only the object key, obj url into the queue and then whichever service requires it will fetch it over the network from the url. we dont add high size data into the queue, its usually in the order of KBs.

* Apache Kafka :
     - https://kafka.apache.org/
     - Open Source: built at Linkedin, now part of Apache
     - High Throughput(at what scale are you able to consume requests): a cluster can handle up to 10 million msg/s (trillion/day)
     - Ultra Low Latency: claims < 2ms latency from message insertion to consumption
     - Horizontal Scalability: automatically manages partitions across multiple servers(brokers) 1000s of brokers (servers), 100,000s of partitions, petabytes of data
     - Fault Tolerant: automatically replicates messages across multiple servers
     - High Availability: can deploy clusters across availability zones
     - Persistent: saves the messages on disk to ensure they're not lost.
     - **Message Ordering: provides strict message ordering (within each partition only)**

* **a message can be consumed by only 1 consumer(server) of a particular consumer group, but can be consumed by different consumer groups(services). kafka maintains a consumer group id.**
  
* if the no. of partitions < no. of consumers in a particular consumer group
  - some of the consumers in that group will have to sit idle.
  - hence, no of partitions is always >= no of consumers in a group.


* **Also, refer this : [Apache Kafka - Medium](https://medium.com/@patelharshali136/apache-kafka-tutorial-kafka-for-beginners-a58140cef84f)**
  
-------------
#############
-------------
-------------
